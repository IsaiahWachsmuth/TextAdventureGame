import React from 'react';

const TranscriptsGrid = ({ transcripts }) => {
  return (
    <section className='transcripts-grid'>
      {/* Display transcripts here */}
    </section>
  );
};

export default TranscriptsGrid;

